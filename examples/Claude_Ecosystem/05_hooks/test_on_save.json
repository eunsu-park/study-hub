{
  "hooks": {
    "FileEditTool": [
      {
        "matcher": "src/**/*.py",
        "command": "python -m pytest tests/ -x --quiet --tb=short 2>&1 | tail -20",
        "description": "Run tests after editing source files (stop on first failure)"
      }
    ],
    "FileWriteTool": [
      {
        "matcher": "src/**/*.py",
        "command": "python -m pytest tests/ -x --quiet --tb=short 2>&1 | tail -20",
        "description": "Run tests after creating new source files"
      }
    ]
  }
}
