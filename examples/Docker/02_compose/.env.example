# Why: .env.example is committed to Git as a template — the real .env (with actual secrets) is gitignored, so new developers know which variables to set without exposing credentials
# Flask Environment
FLASK_ENV=production
PORT=5000

# Why: These match the official Postgres image's expected env vars — the image auto-creates the user/db on first boot, eliminating the need for an init.sql script
# PostgreSQL Configuration
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=mydb

# Redis Configuration
REDIS_PASSWORD=redispassword

# Why: Keeping secrets out of docker-compose.yml means the compose file can be safely committed — variable interpolation (${VAR}) pulls values from .env at runtime
# Instructions:
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update the values with your own secure credentials
#
# 3. Never commit .env to version control
#    (already in .gitignore)
